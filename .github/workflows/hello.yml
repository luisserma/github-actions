name: Mi First Action

on: push

jobs:
    build:
        name: Ubuntu Job
        runs-on: ubuntu-latest
        steps:
            - name: Step One
              uses: actions/checkout@v2
            - name: Python Syntax Checker
              uses: cclauss/Find-Python-syntax-errors-action@v0.2.0
    
